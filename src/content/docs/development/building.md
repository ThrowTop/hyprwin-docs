---
title: Building
description: Build the documentation site locally.
---

Install dependencies:

```powershell
npm install
```

Start the local docs server:

```powershell
npm run dev
```

Build the static site:

```powershell
npm run build
```

Preview the built site:

```powershell
npm run preview
```

